.vscode/*
scripts/firebase-config.js
scripts/firebase-init.js

# Issue Collector - example only, config.js is safe to deploy (no secrets)
# scripts/issue-collector/config.js
.env
.env.local
n8n-workflows/.env

# n8n Encryption Key und Credentials (falls lokal gespeichert)
n8n-workflows/credenti